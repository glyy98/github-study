#如何使用github当做云端笔记
1、本地新建文件夹，使用vscode打开
2、编辑文本代码
3、打开终端，ctrl+`，初始化一个新仓库：git init
4、设置添加远程仓库地址
git remote add 【自定义仓库临时名】 【仓库地址】
4、将文本提交到暂存区
git add .
5、提交更改
git commit -m '' 
6、推上去
git push -u 仓库临时名 main分支

#如何将叠加的内容推上去
1、打开vscode新窗口，选择克隆git，回车输入地址，在桌面创建文件夹；这时候就把仓库给拉下来了
2、添加新东西，然后推上去

1、本地已经拉过一次内容了，直接用vscode打开文件夹，添加内容
2、如果仓库之前被更新过了，那就本地（git pull 仓库名 分支名） 一下，将之前新添加的内容拉下来
3、然后就提交到暂存区，推上去

